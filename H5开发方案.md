# 固定资产管理 H5 微应用 — 全面开发方案

> 项目路径：E:\H5\fixed-assets-h5
> 技术栈：Vue 3 + Composition API + Vant 4 + Vite 5 + Express Mock Server
> 运行方式：
pm run dev:full（Vite 端口 3003 + Express 端口 3002）
> 最新构建：416 modules，0 errors
> 当前状态：V9 BUG 修复完成，待进入钉钉接入与生产部署

---

## 一、项目架构

`
fixed-assets-h5/
├── server/
│   └── index.js              # Express Mock Server（所有 API + 数据）
├── src/
│   ├── main.js               # 入口
│   ├── App.vue               # 根组件
│   ├── router/index.js       # 路由配置（含角色守卫）
│   ├── stores/auth.js        # Pinia 认证状态
│   ├── api/                  # API 封装
│   │   ├── request.js        # Axios 实例
│   │   ├── auth.js           # 登录/用户管理
│   │   ├── assets.js         # 资产 CRUD
│   │   ├── approvals.js      # 申请/审批
│   │   ├── config.js         # 分类/地点/预警/编码规则
│   │   ├── dashboard.js      # 数据面板
│   │   ├── debts.js          # 欠款管理
│   │   ├── inventory.js      # 盘点
│   │   └── export.js         # 导出
│   ├── views/                # 页面组件
│   │   ├── EntryPage.vue     # 登录页
│   │   ├── l1/               # 员工端（L1）
│   │   │   ├── HomePage.vue
│   │   │   ├── AssetsPage.vue
│   │   │   ├── AssetDetailPage.vue
│   │   │   ├── ApplyPage.vue
│   │   │   └── MyApplicationsPage.vue
│   │   ├── l2/               # 管理员端（L2）
│   │   │   ├── HomePage.vue
│   │   │   ├── AssetsPage.vue / AssetRegisterPage.vue / AssetDetailPage.vue
│   │   │   ├── ApprovalsPage.vue
│   │   │   ├── InventoryPage.vue / InventoryDetailPage.vue
│   │   │   └── WorkbenchPage.vue
│   │   ├── l3/               # 超管端（L3）
│   │   │   ├── HomePage.vue
│   │   │   ├── CategoriesPage.vue        # 资产分类管理
│   │   │   ├── LocationsPage.vue         # 存放地点管理
│   │   │   ├── ApprovalFlowsPage.vue     # 审批流程配置
│   │   │   ├── CodeRulesPage.vue         # 编码规则
│   │   │   ├── AlertConfigPage.vue       # 预警配置
│   │   │   ├── AlertsPage.vue            # 告警中心
│   │   │   ├── AuditLogPage.vue          # 审计日志
│   │   │   └── ProfilePage.vue           # 个人中心+用户管理
│   │   └── debts/            # 欠款管理（L2/L3共用）
│   │       ├── DebtListPage.vue
│   │       └── DebtDetailPage.vue
│   ├── components/           # 公共组件
│   │   ├── common/           # AppShell, DataTable, ExportButton, StatusBadge, TreeSelector
│   │   ├── global/AppShell.vue
│   │   ├── l1/L1HomeCards.vue
│   │   ├── l2/ApprovalOverview.vue, DashboardPanel.vue
│   │   └── l3/AuditLogViewer.vue
│   ├── utils/
│   │   ├── constants.js      # 常量定义
│   │   └── tree.js           # 树结构工具
│   └── styles/               # （预留全局样式）
└── dist/                     # 构建输出
`

---

## 二、三端权限模型

| 角色 | 路由前缀 | 职责范围 | 典型用户 |
|------|---------|---------|---------|
| L1（员工） | /l1/ | 领用/归还/报修/报废申请、查看名下资产、查看申请记录 | 张三、李四、王五、赵六 |
| L2（管理员） | /l2/ | 资产管理(CRUD)、审批、盘点、欠款管理、工作台 | 管理员(u004) |
| L3（超管） | /l3/ | 系统配置(分类/地点/审批流程/编码规则/预警)、用户权限管理、审计日志、数据总览 | 系统管理员 |

认证方式：/api/auth/login -> 返回 token + role，路由守卫按 role 拦截跳转。

---

## 三、V9 BUG 修复完成清单

### 3.1 服务端 server/index.js（7 项修复）

| # | 修复内容 | 涉及函数/数据 | 说明 |
|---|---------|-------------|------|
| F1 | 申请列表状态筛选 | /api/applications/mine + /api/applications/my | 增加 statusFilter 参数过滤，前端传什么状态就筛什么 |
| F2 | 数据面板字段补齐 | /api/l3/dashboard | 增加 	otalSurplus、	otalDeficit 从盘点数据计算 |
| F3 | 资产分类数据重构 | categories 数组 | 详见 3.1.1 |
| F4 | 用户更新接口 | PUT /api/users/:id | 支持 L3 修改 L1/L2 用户角色 |
| F5 | 预警设置保存接口 | POST /api/config/alert-settings | 支持保存预警阈值配置 |
| F6 | 欠款权限配置 | debtPermittedUsers + /api/users/check-debt-access | 指定哪些 L1 用户可看到欠款入口 |
| F7 | 资产数据引用更新 | 资产 c2/c3 的 categoryId | A013->ZC0772、A06->ZC079 |

#### 3.1.1 资产分类重构详情

**低值易耗品 (ZC07)** — 原 8 个子项 -> 现 9 个子项：

`
ZC07 低值易耗品
├── ZC071 办公用品
├── ZC072 生产用品
├── ZC073 生活用品
├── ZC074 调修工具
├── ZC075 其它用品
├── ZC076 其它易耗品
├── ZC077 打印耗材类          -- 从 A01 迁移，ID 改为 ZC 编码
│   ├── ZC0771 A4打印纸
│   └── ZC0772 激光打印机硒鼓
├── ZC078 清洁用品            -- 从 A05 迁移
└── ZC079 劳保消防            -- 从 A06 迁移
`

**办公用品类 (A)** — 原 7 个子项 -> 现 2 个子项：

`
A 办公用品类                  -- 原名"耗材分类"
├── A02 日常办公用品
└── A04 其它办公用品           -- 新增
`

已删除：A03（办公用品 — 与 ZC071 名称重复）

### 3.2 L1 层修复（3 项）

| 文件 | 修复内容 | 说明 |
|------|---------|------|
| ApplyPage.vue | 备注字段模板乱码 | maxlength="200" show-word-limit placeholder="请输入备注（选填）" 从漂浮状态移到 	ype="textarea" 行 |
| HomePage.vue | 默认折叠 | ctiveNames 从 ['assets', 'applications'] 改为 [] |
| HomePage.vue | 欠款权限控制 | 新增 checkDebtAccess 接口调用，-if="hasDebtAccess" 控制按钮显示 |

### 3.3 L2 层修复（1 项）

| 文件 | 修复内容 | 说明 |
|------|---------|------|
| AssetDetailPage.vue | 缺少 ref 导入 | 新增 import { ref, onMounted } from 'vue'；之前只有 onMounted 没有 ef，导致 ef is not defined 报错 |

### 3.4 L3 层修复（6 项）

| 文件 | 修复内容 | 说明 |
|------|---------|------|
| HomePage.vue | 统计导航修正 | "待审批"从 /l3/alerts 改为 /l3/approvals |
| HomePage.vue | 删除重复维修卡片 | 告警统计区只有 1 个维修卡片（原来有 2 个） |
| HomePage.vue | 删除冗余数据总览 | overview-grid 整段从模板中移除 |
| ApprovalFlowsPage.vue | 审批人显示中文名 | 新增 getApproverName() 函数解析 ID 为姓名；新增 showApproverPicker Picker 选择器 |
| AlertConfigPage.vue | 配置 UI | 从只读列表改为阈值配置表单（启用开关/低库存预警值/检定提醒天数）+ 保存功能 |
| ProfilePage.vue | 用户管理 | L3 可见"人员管理"卡片，开关切换 L1/L2 角色 |

### 3.5 欠款管理修复（2 项）

| 文件 | 修复内容 | 说明 |
|------|---------|------|
| DebtListPage.vue | 新增欠款表单增加字段 | 联系人/联系电话；"责任人"改"登记人"；统计面板显示总欠款/已还/未还 |
| DebtDetailPage.vue | Picker 列修复 | paymentMethods 从字符串数组改为 {text, value} 对象数组；onMethodConfirm 用 .value 取值 |
| DebtDetailPage.vue | 联系信息显示 | 欠款单位下显示联系人、联系电话；增加还款记录列表 |

### 3.6 路由与 API 修复（3 项）

| 文件 | 修复内容 |
|------|---------|
| outer/index.js | 新增 /l3/approvals -> L2 ApprovalsPage.vue |
| pi/auth.js | 新增 updateUser(id, data) |
| pi/debts.js | 新增 checkDebtAccess, getDebtPermissions, saveDebtPermissions |

---

## 四、已知遗留问题（10 项）

### 4.1 存放地点三级分类

- LocationsPage.vue 模板已支持 3 级展开，但 server/index.js 的 locations 数据只有 2 级结构
- 需要补充第 3 级数据（如 HX01->HX0101->HX0101-01 等），或由前端在 UI 中通过新增操作动态创建

### 4.2 L3 主页告警卡片跳转

- 四个告警卡片（低库存/维修/检定预警/检定过期）均跳转到 /l3/alerts?type=xxx
- AlertsPage.vue 需要支持按 	ype 参数过滤展示对应类型的告警资产
- 当前告警页没有实现按类型筛选的逻辑

### 4.3 盘点盘盈盘亏

- server/index.js 的 inventoryTasks 数据中 items 字段为空数组
- 	otalSurplus/	otalDeficit 计算逻辑已添加但无实际数据，显示为 0
- 需要完善盘点任务的盘盈盘亏录入流程

### 4.4 欠款管理联想输入

- "欠款单位"字段目前是纯文本输入
- 需要实现：输入时弹出已存在的欠款单位列表供选择（模糊匹配）
- 后端 /api/debts 需要增加 utocomplete 接口返回单位名称列表

### 4.5 欠款管理——已核销记录增加还款途径

- V9 要求：已核销的记录增加还款途径
- 目前 DebtDetailPage.vue 已核销状态不能操作还款
- 需要：已核销状态下展示还款途径的填写入口（记录回溯补填）

### 4.6 L3 欠款管理配置

- V9 要求：L3 中设置能看到欠款管理的 L1 人员
- debtPermittedUsers 接口已就绪，需要增加配置 UI
- 可在 L3 ProfilePage 或新增配置页实现

### 4.7 L1 首页 UI 优化

- V9 要求：两条栏目（我名下的资产、我的申请记录）增加 UI 区别显示
- 目前背景纯白，需要增加卡片阴影或背景色区分
- HomePage.vue 中两个 an-collapse-item 需要自定义样式

### 4.8 审批流程——审批人字段解析

- ApprovalFlowsPage.vue 已实现中文名显示和 Picker 选择
- 但需要确保 loadUsers() 正确加载用户列表
- 审批流程保存/加载时的 approverId 与中文名双向解析

### 4.9 预警设置——数据持久化

- 目前预警设置通过 POST /api/config/alert-settings 保存但不写入任何持久化存储
- 需要将阈值设置保存到内存变量中，重启后需重新设置
- 简单方案：在 server/index.js 中添加 ar alertSettings = {...} 全局变量

### 4.10 服务端错误处理

- 部分路由缺少 try-catch，异常会导致整个服务崩溃
- 需要给所有 async handler 添加统一错误处理中间件
- 建议使用 express-async-errors 或包装函数

---

## 五、数据面板统计方案

### 5.1 L3 主页统计数据源

| 统计项 | 数据来源 | 表达式 |
|--------|---------|--------|
| 资产总数 | ssets.length | 	otalAssets |
| 在用 | ssets.filter(s = IN_USE) | inUseCount |
| 闲置 | ssets.filter(s = IDLE) | idleCount |
| 待审批 | pplications.filter(s = PENDING).length | pendingCount |
| 低库存 | ssets.filter(a.alertEnabled && qty <= alertMin) | lertCount |
| 维修 | ssets.filter(s = MAINTENANCE).length | maintenanceCount |
| 检定预警 | ssets.filter(s = INSPECTION).length | inspectionCount |
| 检定过期 | ssets.filter(nextInspDate < today).length | expiredCount |
| 盘盈 | inventoryTasks.items.surplus | 	otalSurplus |
| 盘亏 | inventoryTasks.items.deficit | 	otalDeficit |

### 5.2 检定状态的特殊处理

检定 (INSPECTION) 本质是在用 (IN_USE) 的一个分支，但需要在 UI 中独立展示和提醒。

`
在用一个资产的状态流转：
在用(IN_USE) -> 检定中(INSPECTION) -> 在用(IN_USE)
                 |-> 到期自动提醒
               检定过期(过期日期 < 今天)
`

前端展示规则：
- L2/L3 统计面板："检定中"卡片显示 inspectionCount
- "检定过期"卡片显示 expiredCount
- 资产详情页：在检定周期 > 0 时显示检定信息区块（上次检定/下次检定日期）
- 首页告警：检定预警和检定过期作为独立卡片

---

## 六、代码风格规范

| 规则 | 标准 |
|------|------|
| Vue 文件 | <script setup> Composition API |
| 命名—组件 | PascalCase（AssetDetailPage.vue） |
| 命名—变量 | camelCase（ctiveNames） |
| 命名—常量 | UPPER_SNAKE_CASE |
| 样式 | <style scoped> |
| 数据管理 | Pinia（全局认证状态） |
| API 请求 | Axios + @/api/ 模块化封装 |
| 路由守卫 | outer.beforeEach 按 meta.role 拦截 |
| 服务端 | CommonJS-free ES modules（"type": "module"） |
| 函数风格 | sync/await + try-catch |

---

## 七、后续开发路线图

`
Phase 1: V9 BUG 修复（当前阶段 DONE）
  +-- 服务端修复 DONE
  +-- L1/L2/L3 页面修复 DONE
  +-- 路由/API 补全 DONE
  +-- 构建验证 DONE

Phase 2: 遗留问题修复（4.1 - 4.10）
  +-- 存放地点三级数据
  +-- 告警页面类型筛选
  +-- 盘盈盘亏数据完善
  +-- 欠款联想输入+已核销回溯
  +-- L3 欠款人员配置 UI
  +-- L1 首页 UI 美化
  +-- 审批流程保存/加载验证
  +-- 预警设置持久化

Phase 3: 钉钉微应用接入
  +-- 钉钉 JSAPI 鉴权
  +-- 钉钉免登
  +-- 钉钉工作台入口配置
  +-- 钉钉消息推送（审批通知/预警提醒）
  +-- 移动端适配优化

Phase 4: 测试体系
  +-- 单元测试（Vitest）
  +-- 核心组件快照测试（3+ 组件）
  +-- API Mock 测试
  +-- E2E 流程测试

Phase 5: 生产部署
  +-- Vite 打包优化
  +-- 环境变量配置（.env.production）
  +-- Nginx 反向代理
  +-- 真机调试
  +-- 上线检查清单
`

---

## 八、关键 API 端点总表

`
认证:
  POST /api/auth/login                   -> { token, role, user }
  GET  /api/users                        -> user[]
  PUT  /api/users/:id                    -> update user

资产:
  GET  /api/assets[?status=&categoryId=&custodian=]  -> asset[]
  GET  /api/assets/:id                   -> asset
  POST /api/assets                       -> create
  PUT  /api/assets/:id                   -> update
  DELETE /api/assets/:id                 -> delete
  GET  /api/assets/:id/trace             -> trace log[]
  PUT  /api/assets/:id/inspection-complete -> set INSPECTION->IN_USE
  PUT  /api/assets/:id/repair-complete   -> set MAINTENANCE->IN_USE

申请/审批:
  GET  /api/applications[?status=]       -> application[]
  POST /api/applications                 -> create
  GET  /api/applications/mine[?status=&userId=]  -> my apps
  GET  /api/applications/:id/detail      -> app detail
  PUT  /api/applications/:id/approve     -> approve
  PUT  /api/applications/:id/reject      -> reject
  GET  /api/applications/pending-list    -> pending[]
  GET  /api/applications/pending-count   -> { count }

分类/地点/配置:
  GET/POST/PUT/DELETE /api/config/categories/:id?
  GET/POST/PUT/DELETE /api/config/locations/:id?
  GET/POST /api/config/approval-mapping
  GET/POST /api/config/code-rules
  GET/POST /api/config/alert-settings
  GET /api/config/audit-logs[?page=&pageSize=]

数据面板:
  GET /api/dashboard                     -> L2 stats
  GET /api/l3/dashboard                  -> L3 stats (含 totalSurplus/deficit)

欠款:
  GET /api/debts[?keyword=&status=&page=&pageSize=]
  GET /api/debts/:id
  POST /api/debts
  PUT /api/debts/:id
  DELETE /api/debts/:id
  POST /api/debts/:id/repay

盘点:
  GET /api/inventory/tasks
  POST /api/inventory/tasks

导出:
  GET /api/export/assets
  GET /api/export/debts
  GET /api/export/audit-logs

欠款权限:
  GET/POST /api/config/debt-permissions
  GET /api/users/check-debt-access?userId=
`

---

## 九、启动与验证

`ash
# 开发启动（前后端同时）
cd E:\H5\fixed-assets-h5
npm run dev:full

# 单独构建前端
npx vite build

# 访问入口
http://localhost:3003/

# 测试账号
L1: u001(张三) / u002(李四) / u003(王五) / u004(赵六) -> 密码 123456
L2: u004(赵六，L2角色) -> 密码 123456
L3: admin(系统管理员) -> 密码 123456
`

---

*生成日期：2026-05-21*
*文档版本：v1.0*